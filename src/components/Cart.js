import { CartState } from '../context/Context'
function Cart() {
    const {state: {cart}, dispatch} = CartState();
    return (
        <div>
            
        </div>
    )
}

export default Cart
